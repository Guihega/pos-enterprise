// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client'
import { client } from './client.gen'
import type {
  AdjustStockData,
  AdjustStockErrors,
  AdjustStockResponses,
  CancelSaleData,
  CancelSaleErrors,
  CancelSaleResponses,
  CloseCashSessionData,
  CloseCashSessionErrors,
  CloseCashSessionResponses,
  CreateCustomerData,
  CreateCustomerErrors,
  CreateCustomerResponses,
  CreateProductData,
  CreateProductErrors,
  CreateProductResponses,
  CreateSaleData,
  CreateSaleErrors,
  CreateSaleResponses,
  DeleteCustomerData,
  DeleteCustomerErrors,
  DeleteCustomerResponses,
  DeleteProductData,
  DeleteProductErrors,
  DeleteProductResponses,
  GetCashSessionReportData,
  GetCashSessionReportErrors,
  GetCashSessionReportResponses,
  GetCustomerData,
  GetCustomerErrors,
  GetCustomerResponses,
  GetProductData,
  GetProductErrors,
  GetProductResponses,
  GetSalesSummaryData,
  GetSalesSummaryErrors,
  GetSalesSummaryResponses,
  HealthCheckData,
  HealthCheckResponses,
  ListBrandsData,
  ListBrandsErrors,
  ListBrandsResponses,
  ListCashRegistersData,
  ListCashRegistersErrors,
  ListCashRegistersResponses,
  ListCashSessionsData,
  ListCashSessionsErrors,
  ListCashSessionsResponses,
  ListCategoriesData,
  ListCategoriesErrors,
  ListCategoriesResponses,
  ListCustomersData,
  ListCustomersErrors,
  ListCustomersResponses,
  ListInventoryMovementsData,
  ListInventoryMovementsErrors,
  ListInventoryMovementsResponses,
  ListInventoryStocksData,
  ListInventoryStocksErrors,
  ListInventoryStocksResponses,
  ListProductsData,
  ListProductsErrors,
  ListProductsResponses,
  ListTaxesData,
  ListTaxesErrors,
  ListTaxesResponses,
  ListUnitsData,
  ListUnitsErrors,
  ListUnitsResponses,
  ListWarehousesData,
  ListWarehousesErrors,
  ListWarehousesResponses,
  LoginData,
  LoginErrors,
  LoginResponses,
  LogoutAllData,
  LogoutAllErrors,
  LogoutAllResponses,
  LogoutData,
  LogoutErrors,
  LogoutResponses,
  MeData,
  MeErrors,
  MeResponses,
  OpenCashSessionData,
  OpenCashSessionErrors,
  OpenCashSessionResponses,
  PinVerifyData,
  PinVerifyErrors,
  PinVerifyResponses,
  UpdateCustomerData,
  UpdateCustomerErrors,
  UpdateCustomerResponses,
  UpdateProductData,
  UpdateProductErrors,
  UpdateProductResponses,
} from './types.gen'

export type Options<
  TData extends TDataShape = TDataShape,
  ThrowOnError extends boolean = boolean,
  TResponse = unknown,
> = Options2<TData, ThrowOnError, TResponse> & {
  /**
   * You can provide a client instance returned by `createClient()` instead of
   * individual options. This might be also useful if you want to implement a
   * custom client.
   */
  client?: Client
  /**
   * You can pass arbitrary values through the `meta` object. This can be
   * used to access values that aren't defined as part of the SDK function.
   */
  meta?: Record<string, unknown>
}

/**
 * Estado del servicio
 *
 * Devuelve el estado del proceso de la API. No requiere autenticación
 * ni tenant. Útil para orquestadores (Docker, Kubernetes) y monitoring.
 *
 */
export const healthCheck = <ThrowOnError extends boolean = false>(
  options?: Options<HealthCheckData, ThrowOnError>,
) =>
  (options?.client ?? client).get<HealthCheckResponses, unknown, ThrowOnError>({
    url: '/health',
    ...options,
  })

/**
 * Login por email y contraseña
 *
 * Autentica al usuario en el tenant indicado por el header `X-Tenant`
 * y devuelve un token Bearer de Sanctum.
 *
 * El email se normaliza al recibirse (`strtolower` + `trim`).
 *
 */
export const login = <ThrowOnError extends boolean = false>(
  options: Options<LoginData, ThrowOnError>,
) =>
  (options.client ?? client).post<LoginResponses, LoginErrors, ThrowOnError>({
    url: '/auth/login',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  })

/**
 * Usuario autenticado actual
 *
 * Devuelve los datos del usuario dueño del token.
 */
export const me = <ThrowOnError extends boolean = false>(options: Options<MeData, ThrowOnError>) =>
  (options.client ?? client).get<MeResponses, MeErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/auth/me',
    ...options,
  })

/**
 * Cerrar sesión actual
 *
 * Revoca el token actual. No afecta a otras sesiones del usuario.
 */
export const logout = <ThrowOnError extends boolean = false>(
  options: Options<LogoutData, ThrowOnError>,
) =>
  (options.client ?? client).post<LogoutResponses, LogoutErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/auth/logout',
    ...options,
  })

/**
 * Cerrar todas las sesiones
 *
 * Revoca TODOS los tokens del usuario (todos los dispositivos).
 */
export const logoutAll = <ThrowOnError extends boolean = false>(
  options: Options<LogoutAllData, ThrowOnError>,
) =>
  (options.client ?? client).post<LogoutAllResponses, LogoutAllErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/auth/logout-all',
    ...options,
  })

/**
 * Verificar PIN supervisor
 *
 * Verifica el PIN del usuario autenticado. Útil para autorizar acciones
 * sensibles (ej: cancelar una venta) sin re-autenticar la sesión.
 *
 * Tras 10 intentos fallidos el PIN queda bloqueado 15 minutos.
 *
 */
export const pinVerify = <ThrowOnError extends boolean = false>(
  options: Options<PinVerifyData, ThrowOnError>,
) =>
  (options.client ?? client).post<PinVerifyResponses, PinVerifyErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/auth/pin-verify',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  })

/**
 * Lista productos del catálogo
 *
 * Devuelve productos paginados. Soporta búsqueda por nombre/SKU/código
 * de barras (`q`), filtros por estado, categoría y marca, y orden.
 *
 * Cada producto incluye sus relaciones precargadas (`category`, `brand`,
 * `unit`, `tax`). Para el detalle completo (con `barcodes` e `images`),
 * usa `GET /products/{uuid}`.
 *
 */
export const listProducts = <ThrowOnError extends boolean = false>(
  options: Options<ListProductsData, ThrowOnError>,
) =>
  (options.client ?? client).get<ListProductsResponses, ListProductsErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/products',
    ...options,
  })

/**
 * Crea un producto en el catalogo
 *
 * Crea un producto. Requiere permiso `product.create`. El `sku` debe ser
 * unico dentro del tenant. Las relaciones (`category_uuid`, `brand_uuid`,
 * `unit_uuid`, `tax_uuid`) se referencian por UUID y deben pertenecer al
 * tenant en contexto. Devuelve el producto creado con sus relaciones
 * `category`, `brand`, `unit` y `tax` precargadas.
 *
 */
export const createProduct = <ThrowOnError extends boolean = false>(
  options: Options<CreateProductData, ThrowOnError>,
) =>
  (options.client ?? client).post<CreateProductResponses, CreateProductErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/products',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  })

/**
 * Elimina un producto (soft delete)
 *
 * Elimina logicamente un producto (soft delete; el registro se conserva
 * por regulacion contable). Requiere permiso `product.delete`. Responde
 * 204 sin cuerpo.
 *
 */
export const deleteProduct = <ThrowOnError extends boolean = false>(
  options: Options<DeleteProductData, ThrowOnError>,
) =>
  (options.client ?? client).delete<DeleteProductResponses, DeleteProductErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/products/{uuid}',
    ...options,
  })

/**
 * Detalle de un producto
 *
 * Devuelve un producto con todas sus relaciones (`category`, `brand`,
 * `unit`, `tax`, `barcodes`, `images`).
 *
 */
export const getProduct = <ThrowOnError extends boolean = false>(
  options: Options<GetProductData, ThrowOnError>,
) =>
  (options.client ?? client).get<GetProductResponses, GetProductErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/products/{uuid}',
    ...options,
  })

/**
 * Actualiza un producto
 *
 * Actualiza parcialmente un producto. Requiere permiso `product.update`.
 * Los campos son opcionales; los enviados se validan. El `sku` sigue
 * siendo unico por tenant (se excluye el propio producto). Devuelve el
 * producto actualizado con `category`, `brand`, `unit` y `tax`.
 *
 */
export const updateProduct = <ThrowOnError extends boolean = false>(
  options: Options<UpdateProductData, ThrowOnError>,
) =>
  (options.client ?? client).patch<UpdateProductResponses, UpdateProductErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/products/{uuid}',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  })

/**
 * Lista categorias del catalogo
 *
 * Devuelve categorias paginadas del tenant. Usado para poblar
 * selects en formularios de producto. Requiere permiso `product.view`.
 *
 */
export const listCategories = <ThrowOnError extends boolean = false>(
  options: Options<ListCategoriesData, ThrowOnError>,
) =>
  (options.client ?? client).get<ListCategoriesResponses, ListCategoriesErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/categories',
    ...options,
  })

/**
 * Lista marcas del catalogo
 *
 * Devuelve marcas paginadas del tenant. Usado para poblar
 * selects en formularios de producto. Requiere permiso `product.view`.
 *
 */
export const listBrands = <ThrowOnError extends boolean = false>(
  options: Options<ListBrandsData, ThrowOnError>,
) =>
  (options.client ?? client).get<ListBrandsResponses, ListBrandsErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/brands',
    ...options,
  })

/**
 * Lista unidades de medida del catalogo
 *
 * Devuelve unidades de medida paginadas del tenant. Usado para poblar
 * selects en formularios de producto. Requiere permiso `product.view`.
 *
 */
export const listUnits = <ThrowOnError extends boolean = false>(
  options: Options<ListUnitsData, ThrowOnError>,
) =>
  (options.client ?? client).get<ListUnitsResponses, ListUnitsErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/units',
    ...options,
  })

/**
 * Lista impuestos del catalogo
 *
 * Devuelve impuestos paginadas del tenant. Usado para poblar
 * selects en formularios de producto. Requiere permiso `product.view`.
 *
 */
export const listTaxes = <ThrowOnError extends boolean = false>(
  options: Options<ListTaxesData, ThrowOnError>,
) =>
  (options.client ?? client).get<ListTaxesResponses, ListTaxesErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/taxes',
    ...options,
  })

/**
 * Lista cajas registradoras del tenant
 *
 * Devuelve las cajas registradoras del tenant, paginadas.
 * Filtros: `branch` (UUID), `active` (bool).
 * Cada caja incluye su `branch` precargado.
 *
 */
export const listCashRegisters = <ThrowOnError extends boolean = false>(
  options: Options<ListCashRegistersData, ThrowOnError>,
) =>
  (options.client ?? client).get<ListCashRegistersResponses, ListCashRegistersErrors, ThrowOnError>(
    {
      security: [
        { scheme: 'bearer', type: 'http' },
        { name: 'X-Tenant', type: 'apiKey' },
      ],
      url: '/cash/registers',
      ...options,
    },
  )

/**
 * Lista sesiones de caja
 *
 * Devuelve sesiones de caja paginadas (más recientes primero por
 * `opened_at`). Para saber si hay una sesión activa para el
 * usuario/caja, filtrar con `status=open`.
 *
 */
export const listCashSessions = <ThrowOnError extends boolean = false>(
  options: Options<ListCashSessionsData, ThrowOnError>,
) =>
  (options.client ?? client).get<ListCashSessionsResponses, ListCashSessionsErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/cash/sessions',
    ...options,
  })

/**
 * Abre una sesión de caja
 *
 * Crea una sesión de caja para la caja registradora indicada con
 * el monto inicial en efectivo. Devuelve la sesión recién creada
 * con su `uuid` (necesario para el checkout del POS).
 *
 * Si la caja ya tiene una sesión abierta, responde 409 con código
 * `SESSION_ALREADY_OPEN`.
 *
 */
export const openCashSession = <ThrowOnError extends boolean = false>(
  options: Options<OpenCashSessionData, ThrowOnError>,
) =>
  (options.client ?? client).post<OpenCashSessionResponses, OpenCashSessionErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/cash/sessions/open',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  })

/**
 * Cierra una sesion de caja
 *
 * Cierra la sesion de caja indicada calculando el arqueo. El
 * backend calcula `expected_amount` como el monto de apertura mas
 * la suma de los movimientos que afectan al efectivo (ventas en
 * efectivo, entradas y salidas). El cajero provee `counted_amount`
 * (lo contado fisicamente) y el backend calcula
 * `difference = counted_amount - expected_amount`.
 *
 * Devuelve la sesion cerrada con el bloque `closing` poblado. Si
 * la sesion ya no esta abierta, responde 409 con codigo
 * `SESSION_NOT_OPEN`.
 *
 */
export const closeCashSession = <ThrowOnError extends boolean = false>(
  options: Options<CloseCashSessionData, ThrowOnError>,
) =>
  (options.client ?? client).post<CloseCashSessionResponses, CloseCashSessionErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/cash/sessions/{session}/close',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  })

/**
 * Corte de caja (X/Z) de una sesion
 *
 * Devuelve el corte de caja de la sesion indicada: ventas
 * completadas del turno, desglose de pagos por metodo, desglose
 * de movimientos de caja por tipo, y el resumen de efectivo
 * (apertura, delta de movimientos, esperado).
 *
 * Si la sesion esta `open`, es un corte X "en vivo":
 * `counted_amount` y `difference` son `null`. Si la sesion esta
 * `closed`, es un corte Z que refleja el cierre ya persistido por
 * `POST .../close`.
 *
 */
export const getCashSessionReport = <ThrowOnError extends boolean = false>(
  options: Options<GetCashSessionReportData, ThrowOnError>,
) =>
  (options.client ?? client).get<
    GetCashSessionReportResponses,
    GetCashSessionReportErrors,
    ThrowOnError
  >({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/cash/sessions/{session}/report',
    ...options,
  })

/**
 * Cancelar una venta completada
 *
 * Anula una venta con status `completed`. Revierte stock, pagos en efectivo
 * y credito de cliente. Requiere permiso `sale.void` y motivo obligatorio.
 * Solo el usuario autenticado puede ejecutar esta accion; el PIN supervisor
 * se verifica en el frontend antes de llamar este endpoint.
 *
 */
export const cancelSale = <ThrowOnError extends boolean = false>(
  options: Options<CancelSaleData, ThrowOnError>,
) =>
  (options.client ?? client).post<CancelSaleResponses, CancelSaleErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/sales/{uuid}/cancel',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  })

/**
 * Registra una venta (checkout)
 *
 * Registra una venta completa de forma atomica: items, pagos,
 * impuestos, descuentos y descuento de stock. Si el pago no cubre
 * el total (o si hay sobrepago con un metodo distinto de efectivo),
 * responde 422 con `PAYMENT_MISMATCH`. Si no hay stock suficiente
 * para algun item, 409 `INSUFFICIENT_STOCK`. Si la sesion de caja
 * ya no esta abierta, 409 `SESSION_NOT_OPEN`. Si el pago incluye
 * metodo `credit` y el cliente no tiene credito disponible, 402
 * `INSUFFICIENT_CREDIT`.
 *
 * Devuelve el recurso de venta completo con `items`, `payments`,
 * `taxes`, `customer`, `cashier` y los totales calculados
 * (incluyendo `change` cuando aplica).
 *
 */
export const createSale = <ThrowOnError extends boolean = false>(
  options: Options<CreateSaleData, ThrowOnError>,
) =>
  (options.client ?? client).post<CreateSaleResponses, CreateSaleErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/sales',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  })

/**
 * Lista existencias (stock) del tenant
 *
 * Devuelve existencias paginadas por almacen y producto. Para el POS,
 * filtrar por `warehouse` (UUID del almacen default) para obtener el
 * stock disponible (`quantity.available`) de cada producto y bloquear
 * ventas sin existencias en la UI. El backend valida el stock real al
 * cobrar; este endpoint es para UX preventiva.
 *
 */
export const listInventoryStocks = <ThrowOnError extends boolean = false>(
  options: Options<ListInventoryStocksData, ThrowOnError>,
) =>
  (options.client ?? client).get<
    ListInventoryStocksResponses,
    ListInventoryStocksErrors,
    ThrowOnError
  >({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/inventory/stocks',
    ...options,
  })

/**
 * Ajusta existencias de un producto en un almacen
 *
 * Aplica un ajuste manual de existencias por `delta` (positivo o
 * negativo, nunca cero) con un motivo obligatorio. Crea un movimiento
 * de inventario de tipo `adjustment` y devuelve ese movimiento.
 * Requiere permiso `inventory.adjust`. Si el ajuste dejaria el stock
 * en negativo, responde `409 INSUFFICIENT_STOCK`.
 *
 */
export const adjustStock = <ThrowOnError extends boolean = false>(
  options: Options<AdjustStockData, ThrowOnError>,
) =>
  (options.client ?? client).post<AdjustStockResponses, AdjustStockErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/inventory/adjust',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  })

/**
 * Kardex de movimientos de inventario
 *
 * Devuelve movimientos paginados ordenados del mas reciente al mas
 * antiguo. Filtros: `product`, `warehouse`, `type`, `from`, `to`
 * (rango por fecha de movimiento). Requiere permiso `inventory.view`.
 *
 */
export const listInventoryMovements = <ThrowOnError extends boolean = false>(
  options: Options<ListInventoryMovementsData, ThrowOnError>,
) =>
  (options.client ?? client).get<
    ListInventoryMovementsResponses,
    ListInventoryMovementsErrors,
    ThrowOnError
  >({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/inventory/movements',
    ...options,
  })

/**
 * Lista almacenes del tenant
 *
 * Devuelve almacenes paginados ordenados por nombre. Filtros:
 * `branch` (UUID de sucursal) y `active`. Util para poblar selectores
 * de almacen en ajustes y kardex. Requiere permiso `inventory.view`.
 *
 */
export const listWarehouses = <ThrowOnError extends boolean = false>(
  options: Options<ListWarehousesData, ThrowOnError>,
) =>
  (options.client ?? client).get<ListWarehousesResponses, ListWarehousesErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/warehouses',
    ...options,
  })

/**
 * Lista clientes
 *
 * Devuelve clientes paginados. Soporta busqueda libre (`q`) por
 * nombre, code, tax_id, email; filtros por tipo, credito, activos y
 * bloqueados; y orden por nombre, fecha o saldo.
 *
 */
export const listCustomers = <ThrowOnError extends boolean = false>(
  options: Options<ListCustomersData, ThrowOnError>,
) =>
  (options.client ?? client).get<ListCustomersResponses, ListCustomersErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/customers',
    ...options,
  })

/**
 * Crea un cliente
 *
 * Crea un cliente. Requiere permiso `customer.create`. Los campos
 * `code`, `tax_id` y `email` deben ser unicos dentro del tenant.
 *
 */
export const createCustomer = <ThrowOnError extends boolean = false>(
  options: Options<CreateCustomerData, ThrowOnError>,
) =>
  (options.client ?? client).post<CreateCustomerResponses, CreateCustomerErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/customers',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  })

/**
 * Elimina un cliente (soft delete)
 *
 * Elimina un cliente. Requiere permiso `customer.delete`. Si el cliente
 * tiene saldo deudor (`credit.balance` mayor a cero) la API responde
 * `409 CUSTOMER_HAS_BALANCE` y no lo elimina.
 *
 */
export const deleteCustomer = <ThrowOnError extends boolean = false>(
  options: Options<DeleteCustomerData, ThrowOnError>,
) =>
  (options.client ?? client).delete<DeleteCustomerResponses, DeleteCustomerErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/customers/{uuid}',
    ...options,
  })

/**
 * Detalle de un cliente
 */
export const getCustomer = <ThrowOnError extends boolean = false>(
  options: Options<GetCustomerData, ThrowOnError>,
) =>
  (options.client ?? client).get<GetCustomerResponses, GetCustomerErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/customers/{uuid}',
    ...options,
  })

/**
 * Actualiza un cliente
 *
 * Actualiza un cliente. Requiere permiso `customer.update`. Acepta
 * actualizacion parcial. `code`, `tax_id` y `email` siguen siendo
 * unicos dentro del tenant (ignorando al propio cliente).
 *
 */
export const updateCustomer = <ThrowOnError extends boolean = false>(
  options: Options<UpdateCustomerData, ThrowOnError>,
) =>
  (options.client ?? client).put<UpdateCustomerResponses, UpdateCustomerErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/customers/{uuid}',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  })

/**
 * Resumen de ventas de un dia (alimenta reporte y dashboard).
 */
export const getSalesSummary = <ThrowOnError extends boolean = false>(
  options: Options<GetSalesSummaryData, ThrowOnError>,
) =>
  (options.client ?? client).get<GetSalesSummaryResponses, GetSalesSummaryErrors, ThrowOnError>({
    security: [
      { scheme: 'bearer', type: 'http' },
      { name: 'X-Tenant', type: 'apiKey' },
    ],
    url: '/reports/sales-summary',
    ...options,
  })
